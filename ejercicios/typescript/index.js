console.log("hello ts");

function add2(a, b){ //Le aañado tipado al codigo, cosas de ts
    return a + b;
}

const sum2 = add2(2, 3); //Con el cursor sobre sum, me dice que sera un numero, ts hace un analisis estatico al codigo